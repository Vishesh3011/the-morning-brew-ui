import styled from "styled-components";

export const DatedNewsGrid = styled.div`
    margin-top: 4rem;
    margin-left: 20rem;
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    color: var(--color-light);
`